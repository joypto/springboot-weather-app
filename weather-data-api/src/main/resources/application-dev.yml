spring:
  config:
    activate:
      on-profile: dev
    import:
      - "classpath:/application-prod-db.yml"
      - "classpath:/application-secret.yml"
  jpa:
    hibernate:
      ddl-auto: update
logging:
  level:
    root: info
